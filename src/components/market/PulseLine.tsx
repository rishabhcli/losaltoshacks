/**
 * PulseLine — DEPRECATED.
 * Kept as a no-op so existing imports don't break during migration.
 * Renders nothing.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function PulseLine(props: { className?: string; intensity?: "flat" | "normal" | "volatile" }) {
  return null;
}
